using TcHmiSrv.Core;

namespace SQLiteConnector
{
    // Contains extension specific error values.
    enum ExtensionErrorValue
    {
        HmiExtSuccess = 0,
        HmiExtFail = 1,
        ERROR_DUPE_CONFIG = 2
    }
}
